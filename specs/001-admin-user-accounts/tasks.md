# Tasks: Администрирование — управление учётными записями

**Input**: Design documents from `/specs/001-admin-user-accounts/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, quickstart.md

**Tests**: Специальные тест-задачи не выделены — в проекте ручные сценарии (см. quickstart.md, «Проверка» в каждой фазе).

**Organization**: Задачи сгруппированы по user stories. Значительная часть реализации уже собрана в форке (коммиты `b952f67`, `8b5c2a4`) — фазы фиксируют закрытие разрывов со спекой. Метка **«(в редакторе)»** = задача выполняется пользователем в редакторе Appsmith; **«(в форке)»** = внешняя правка ассистента в локальной копии форка (конституция III).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: user story (US1…US6)
- Include exact file paths in descriptions

## Path Conventions

- Страница форка: `pages/Администрирование/` → коллекции: `queries/<query>/{<query>.txt, metadata.json}`, `jsobjects/userAdmin/userAdmin.js`, `widgets/…/*.json`
- Артефакты фичи: `specs/001-admin-user-accounts/`

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Инфраструктура Spec Kit и страница — уже созданы

- [x] T001 Инициализирован Spec Kit в `Appsmith_AF_fork` (`.specify/`, команды `.opencode/commands/speckit.*`, конституция в `.specify/memory/constitution.md`) — выполнено 20.09.2026, коммит `ac9b519`
- [x] T002 Создана спека фичи `specs/001-admin-user-accounts/spec.md` + чек-лист качества — выполнено 20.09.2026, коммит `e889491`
- [x] T003 Страница «Администрирование» собрана в редакторе (запросы, `userAdmin.js`, виджеты) — выполнео 18.09.2026, коммиты `b952f67`, `8b5c2a4`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Общая проверка схемы и контрактов перед правками

- [x] T004 Проверить фактическую схему `appsmith.users` при доступном VPN (psql: наличие UNIQUE на `login`, колонки) и при необходимости обновить `research.md` — закрыто 21.09.2026 по снапшоту БД 17.09.2026: **UNIQUE на `login` нет** → проверка дубликата в JS обязательна (FR-011; research №1, C2 Deferred)
- [x] T005 [P] Сверить тела `save_user.txt`, `delete_user.txt`, `get_users.txt` в форке со спекой (FR-004/Fr-006/Fr-010) — расхождения зафиксировать в журнале: факт 20.09.2026 — логика в форке соответствует спеке, кроме `u.password` в `get_users` (задача T010); закрыто 21.09.2026

**Checkpoint**: Foundation ready — правки user stories можно выполнять в параллели.

---

## Phase 3: User Story 1 - Просмотр списка учётных записей (Priority: P1) 🎯 MVP

**Goal**: Список без паролей: логин, учреждение, роль, активность; сортировка/фильтр/пагинация на нативной таблице.

**Independent Test**: Открыть страницу под администратором, сверить строки с фактическими записями БД; в `tableData`/devtools нет поля `password`.

### Implementation for User Story 1

- [x] T010 [US1] Убрать `u.password` из SELECT запроса `pages/Администрирование/queries/get_users/get_users.txt` (в форке; конституция III — правки SQL в форке разрешены ассистенту). Выполнено 20.09.2026 (коммиты `4ede945`/`2574c57`). **Замечание (22.09.2026):** позже пользователь вернул `u.password` в SELECT и добавил скрытую колонку `password` в `usersTable` (коммит `70585d8`) — пароль нужен форме редактирования; скрытая колонка убрана (`2a634f2`, колонка `ou_inn` вместо неё), но `u.password` в SELECT остался для `userPassword` при редактировании. Хранение открытым текстом — замечание про хэширование остаётся.
- [x] T011 [P] [US1] Колонка `password` убрана из `pages/Администрирование/widgets/AdminForm/usersTable.json` — заменена видимой `ou_inn` (правка пользователя 22.09.2026, коммит `2a634f2`; label колонки «ou_inn» — при желании переименовать «ИНН») (закрыто)
- [ ] T012 [US1] Проверка: список загружается, колонки читаемые (Логин/Роль/Учреждение/Активен/ИНН), без видимой колонки пароля; клик по строке активирует кнопки «Редактировать»/«Удалить» (quickstart, Сценарий 1; в редакторе)

**Checkpoint**: US1 независимо рабочая — список без секретов.

---

## Phase 4: User Story 2 - Добавление нового пользователя (Priority: P1)

**Goal**: Добавление через `userModal`: логин, учреждение (по ИНН, кейс A), роль (Select), пароль (Password Input — обязателен), активность по умолчанию.

**Independent Test**: Создание пользователя через форму и вход по новому логину/паролю; дубликат логина отклоняется с сообщением.

### Implementation for User Story 2

- [x] T020 [P] [US2] **Кейс A «Я знаю ИНН» (решение пользователя 22.09.2026 — Select на ~4000 учреждений отклонён):** поле учреждения в форме — **Text Input «ИНН»** (виджет `userINN`, `userINNName` — название учреждения), поиск при сохранении: `get_ou_by_inn` (точное совпадение по `inn`) → в `userAdmin.saveUser()` брать `directory_inn_id` из найденной строки (в т.ч. вместо ошибочного `directory_inn_id` из текста формы); 0 совпадений — `showAlert('Учреждение с ИНН … не найдено', 'error')`; >1 — `showAlert('ИНН неоднозначен…', 'error')` (UNIQUE на `inn` нет). **Выполнено в форке 22.09.2026, коммит `cf8492c`** (SQL/JS); UX (`onInnChange`, название) — `98cddd3`/`ae5d1e5`; в редакторе — Pull + проверка
- [x] T021 [P] [US2] Поле пароля переведено на **Password Input** (отдельный виджет `userPassword`, inputType PASSWORD — правка пользователя 22.09.2026, коммит `70585d8`). **Пароль обязателен всегда (и при добавлении, и при редактировании)** — валидация в `userAdmin.js` `saveUser()` (коммиты `bd64a4b`, обновлено в `ced16fb` под виджеты); замечание карточки п. 2, SC-005
- [x] T022 [US2] Добавить проверку уникальности логина в `pages/Администрирование/jsobjects/userAdmin/userAdmin.js`, метод `saveUser()`. Выполнено 20–22.09.2026: изначальная проверка по `get_users.data` (коммиты `4ede945`/`2574c57`) **заменена 22.09 точечным запросом `check_user_login`** (`bf68da4`) — перезагрузка всей таблицы очищала форму через пересчёт `selectedRow`; теперь форма сохраняет ввод при дубликате. `FR-011, US2/AC5`
- [ ] T023 [P] [US2] Убедиться, что форма открывает пустую форму для добавления — JS-часть выполнена 22.09.2026: `openUserModal()` сбрасывает `setSelectedRowIndex(-1)`, чистит поля `setValue('')`/`resetWidget('userRole')`, `userEnabled.setValue(true)` (коммиты `ced16fb`, `114a4f6`, `ce62314` — `setSelectedOptionValue` заменён на `resetWidget`). **Осталась проверка в редакторе** (после Pull; quickstart Сценарий 2, п. 1)
- [ ] T024 [US2] Проверка: добавление успешное → вход по новым данным; кнопка «Сохранить» неактивна при пустых полях; пустой логин/пароль/ИНН/роль отклоняются; ИНН не найден → «Учреждение с ИНН … не найдено»; дубликат отклоняется с сообщением без очистки формы (quickstart, Сценарии 2–3; в редакторе)

**Checkpoint**: US1 + US2 работают независимо.

---

## Phase 5: User Story 3 - Редактирование пользователя (Priority: P1)

**Goal**: Редактирование логина/роли/учреждения/активности/пароля в том же окне; пустой пароль = без изменений.

**Independent Test**: Смена логина → вход по новому; пустой пароль не меняет пароль; смена роли отражается.

### Implementation for User Story 3

- [x] T030 [US3] (Зависит от T020) Заполнение учреждения при редактировании: добавлен `di.inn AS ou_inn` в SELECT `pages/Администрирование/queries/get_users/get_users.txt` (в форке, коммит `cf8492c`) → виджет `userINN` при редактировании подставляется из `usersTable.selectedRow.ou_inn`, `userINNName` — из `ou_short_name` (в редакторе — Pull + проверка)
- [x] T031 [US3] Проверить путь «пустой пароль = не менять»: переданный в `save_user` `password: ''` не перезаписывает хранимый (тело `save_user.txt` уже обрабатывает `CASE WHEN {{this.params.password}} = ''` — подтверждено 21.09.2026). **Изменение 22.09.2026 (решение пользователя):** пароль теперь **обязателен всегда** (и при добавлении, и при редактировании) — валидация в `userAdmin.js` `saveUser()` (`bd64a4b`), ветка «пустой пароль» в UI недостижима (FR-003, US3/AC2)
- [ ] T032 [US3] Проверка: смена логина (старый не пускает, новый впускает), смена роли в списке, собственный логин при редактировании не считается дубликатом, пароль при редактировании обязателен (quickstart, Сценарий 4; US3/AC1/AC3/AC4/AC5; в редакторе)

**Checkpoint**: US1–US3 работают независимо.

---

## Phase 6: User Story 4 - Блокировка и разблокировка (Priority: P2)

**Goal**: Блокировка (`is_user_enabled=false`) запрещает вход и гасит активные сессии; разблокировка восстанавливает.

**Independent Test**: Блокировка пользователя с активной сессией — вход отклонён, сессия завершена.

### Implementation for User Story 4

- [x] T040 [P] [US4] Подтвердить, что переключение «Пользователь активен» в `CardForm` (Switch `is_user_enabled`) прокидывается в `save_user` и сессии гасятся (`UPDATE appsmith.user_sessions SET end_time = now() ... AND (NOT ch.is_user_enabled OR ch.password <> '')`) — закрыто 21.09.2026: тело `save_user.txt` соответствует (FR-006, edge case «блокировка + смена пароля»); форма с 22.09 — отдельный виджет `userEnabled` (Switch), метод `isSwitchedOn`
- [x] T041 [P] [US4] Проверить сообщение входа для заблокированного: `authorization.js` содержит `if (!user.is_user_enabled) return this.showError("Учётная запись заблокирована!")` — подтверждено 21.09.2026 (в форке, правка 17.09.2026, коммит `4939426`) (FR-005)
- [ ] T042 [US4] Проверка: блокировка (Switch `userEnabled` выкл) → вход отклонён «Учётная запись заблокирована» + сессии погашены; разблокировка → вход возможен (quickstart, Сценарий 5; в редакторе)

**Checkpoint**: US1–US4 работают независимо.

---

## Phase 7: User Story 5 - Удаление пользователя (Priority: P2)

**Goal**: Удаление только при отсутствии сессий и истории отчётов; при отказе — причина.

**Independent Test**: Удаление никогда не входившего — успешно; входившего/с историей — отказ с причиной.

### Implementation for User Story 5

- [x] T050 [P] [US5] Кнопка «ДА» в `pages/Администрирование/widgets/deleteUserModal/` (переименована пользователем 22.09.2026: `Button4.json` → `del_ButtonYES.json`) имеет `onClick: "{{userAdmin.deleteUser()}}"` — подтверждено по коду 22.09.2026 ✅ (замечание карточки п. 4 закрыто)
- [ ] T051 [US5] Проверка по quickstart, Сценарий 6: никогда не входивший — удаляется (US5/AC1); входивший — «Удалить нельзя: он входил в систему» (US5/AC3); с историей отчётов — «Удалить нельзя: у него есть история отчётов» (US5/AC2); сессии в прошлом всё равно блокируют удаление (edge case; в редакторе)

**Checkpoint**: US1–US5 работают независимо.

---

## Phase 8: User Story 6 - Задание пароля администратором (Priority: P3)

**Goal**: Пароль задаёт администратор: 1–11 символов, латиница/цифры; хранение без хэширования (как в системе).

**Independent Test**: Невалидный пароль отклоняется; валидный — вход работает.

### Implementation for User Story 6

- [x] T060 [US6] (Зависит от T021) Подтвердить валидацию пароля в `userAdmin.js` `saveUser()`: `/^[a-zA-Z0-9]{1,11}$/` для введённого пароля + обязательность пароля при добавлении И редактировании — подтверждено по коду 22.09.2026 (`bd64a4b`, финальная версия `bf68da4`) (FR-007, US6/AC1)
- [ ] T061 [US6] Проверка: невалидный пароль отклоняется с сообщением «Пароль: 1–11 символов, только латиница и цифры»; пустой пароль (добавление/редактирование) → «Укажите пароль»; вход с паролем администратора проходит (quickstart, Сценарий 7; в редакторе)

**Checkpoint**: Все user stories функциональны.

---

## Phase N: Polish & Cross-Cutting Concerns

**Purpose**: Финализация перед PR и сверка с регламентом.

- [x] T070 [P] Прогнать все сценарии `specs/001-admin-user-accounts/quickstart.md` (Сценарии 1–7) и зафиксировать результаты в журнале дня (конституция V) — **актуально (23.09.2026):** quickstart обновлён под новую форму (кейс A, отдельные виджеты); прогон — пользователем в редакторе после Pull, фиксация в журнал
- [x] T071 [P] Обновить рабочий слой: карточка [[Администрирование (Учётные записи)]] (факт по форку: `CardForm.formData` в JS, кнопка «ДА» подключена, поле учреждения → Select, пароль → Password Input, `u.password` убран), журнал дня — закрыто 21–22.09.2026 (карточка и журналы актуализированы)
- [ ] T072 Commit & Push в `Appsmith_AF_fork` (push — пользователь, по регламенту; ассистент коммитит локально) → в редакторе **Pull** → проверка → Commit & Push → **PR** в основной `Appsmith_AF` (конституция II) — **PR отложен по решению пользователя 22.09.2026** (до особого распоряжения о PR не напоминать)
- [ ] T073 [P] Проверить, что страница Grammax 46 «Администрирование» описывает итоговую реализацию после merge (обязанность команды; правки документации — после согласования, 19.09.2026) — **после PR** (см. T072)

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: выполнена (коммиты `ac9b519`, `e889491`, `b952f67`, `8b5c2a4`)
- **Foundational (Phase 2)**: T004–T005 — быстрая сверка; не блокирует правки (контракты уже известны)
- **User Stories**: правки реализованы в форке/редакторе, фазы US1–US6 идут по приоритету P1→P2→P3
- **Polish**: после всех user stories

### User Story Dependencies

- **US1 (P1)**: базовая видимость; T010 (убрать `password`) — до проверок без секретов
- **US2 (P1)**: T020 (Select учреждения) и T021 (Password Input) — правки формы (в редакторе); T022 (уникальность логина) — в форке
- **US3 (P1)**: после T010 (нужен `directory_inn_id` в SELECT) и T020 (Select формы)
- **US4 (P2)**: независима (тело `save_user` и `authorization.js` уже корректны)
- **US5 (P2)**: независима (`delete_user` + `deleteUserModal` уже собраны)
- **US6 (P3)**: после T021 (Password Input); валидация regex уже в JS

### Within Each User Story

- Правки формы (Select/Password Input) → в редакторе (пользователь); SQL/JS → в форке (ассистент)
- Проверка каждого сценария — после закрытия задач фазы

### Parallel Opportunities

- T011, T020–T021, T023 — правки разных виджетов в редакторе (но выполняются одним пользователем, последовательно на практике)
- T010 (SQL), T022 (JS) — независимые файлы в форке → можно [P]
- Проверки T040–T041, T050 — сверка уже собранного кода, параллельно с правками

---

## Implementation Strategy

### MVP First (US1 + US2)

1. Phase 2 сверка → T010 (убрать `password`) → T022 (уникальность) в форке
2. T020–T021 правки формы в редакторе
3. Проверка US1+US2 (Сценарии 1–3) — STOP and VALIDATE

### Incremental Delivery

- US1+US2 (добавление и список) — ближайший проверяемый шаг; они же закрывают блокер FR-011
- US3 (редактирование) — после правок формы; US4/US5 — уже готовы, только проверка; US6 — финально

### Parallel Team Strategy

- Ассистент (форк): T010, T022, T040, T041, T050, T060 — SQL/JS
- Пользователь (редактор): T011, T020, T021, T023, T030, T031 — виджеты
- Стыковка: после правок форка — **Pull в редакторе** перед работой с формой (конституция III)

---

## Notes

- **Важно**: форма с 22.09.2026 — **отдельные виджеты** вместо JSON Form `CardForm` (правка пользователя `70585d8`): `userLogin`, `userPassword` (Password Input), `userINN`, `userINNName` (название учреждения по ИНН), `userRole` (Select), `userEnabled` (Switch), `userButtonSave`/`userButtonClose`. `userAdmin.js` читает не `formData`, а поля виджетов (`ced16fb`). Задачи/карточка, упоминающие `CardForm`, историчны.
- **Переименования виджетов (правки пользователя 22.09.2026, коммиты `bab8bda`/`2a634f2`)**: `deleteUserModal`: `Button3`→`del_ButtonNO`, `Button4`→`del_ButtonYES`, `Text2`→`del_Caption`; далее (коммит `70585d8`) → `delButtonNO`/`delButtonYES`/`delCaption`; `userModal`: добавлена `user_ButtonNO` («Закрыть», `closeModal(userModal.name)`), у формы `showReset: false`; порядок полей: Логин(0), Пароль(1), ИНН(2), Роль(3), Активен(4); в `usersTable` колонка `password` удалена, добавлена `ou_inn` (а `u.password` в SELECT оставлен скрытой колонкой для формы редактирования — см. T010).
- **Учреждение — кейс A «Я знаю ИНН»** (решение 22.09.2026): `userINN` Text Input + поиск `get_ou_by_inn` при сохранении, `userINNName` — название по введённому ИНН (`onInnChange`, коммиты `cf8492c`/`98cddd3`/`ae5d1e5`).
- **Уникальность логина** — через точечный запрос `check_user_login` (`bf68da4`), не через `get_users.run()`.
- [P] tasks = different files, no dependencies
- Commit after each task or logical group
- Изменения БД в этой фиче не требуется (уникальность — на уровне приложения, research №1).